import { useAuth } from '../../context/AuthContext'
import { Card } from '../../components/ui/Card'
export default function Profile(){const{user}=useAuth();return <div className="max-w-2xl"><h1 className="text-3xl font-black">Profile</h1><Card className="mt-6 p-6"><div className="flex h-16 w-16 items-center justify-center rounded-full bg-sky-100 text-xl font-black text-sky-700">{user?.name.split(' ').map(x=>x[0]).join('')}</div><dl className="mt-6 space-y-4"><div><dt className="text-xs text-slate-500">Name</dt><dd className="font-bold">{user?.name}</dd></div><div><dt className="text-xs text-slate-500">Email</dt><dd className="font-bold">{user?.email}</dd></div><div><dt className="text-xs text-slate-500">Role</dt><dd className="font-bold capitalize">{user?.role}</dd></div></dl></Card></div>}

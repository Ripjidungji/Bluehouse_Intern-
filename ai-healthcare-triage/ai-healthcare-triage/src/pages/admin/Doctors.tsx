import { doctors } from '../../data/doctors'
export default function Doctors(){return <div><h1 className="text-3xl font-black">Doctors</h1><div className="mt-6 grid gap-4 md:grid-cols-2">{doctors.map(d=><div className="rounded-2xl border bg-white p-5" key={d.id}><h2 className="font-bold">{d.name}</h2><p className="mt-1 text-sm text-slate-500">{d.specialty}</p><p className="mt-4 text-sm">{d.experience} experience</p><span className="mt-4 inline-block rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-700">{d.available?'Available':'Unavailable'}</span></div>)}</div></div>}

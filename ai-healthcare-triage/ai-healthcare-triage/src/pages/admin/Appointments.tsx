import { useAppointments } from '../../hooks/useAppointments'
import { Badge } from '../../components/ui/Badge'
export default function AdminAppointments(){const{items}=useAppointments();return <div><h1 className="text-3xl font-black">Appointments</h1><div className="mt-6 overflow-x-auto rounded-2xl border bg-white"><table className="w-full min-w-[800px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="p-4">Patient</th><th>Doctor</th><th>Date</th><th>Type</th><th>Priority</th><th>Status</th></tr></thead><tbody>{items.map(a=><tr className="border-t" key={a.id}><td className="p-4 font-bold">{a.patientName}</td><td>{a.doctorName}</td><td>{a.date} · {a.time}</td><td>{a.type}</td><td><Badge value={a.priority}/></td><td><Badge value={a.status}/></td></tr>)}</tbody></table></div></div>}

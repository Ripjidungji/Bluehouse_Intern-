export function AssessmentProgress({ step, total }: { step: number; total: number }) { return <div className="mb-8"><div className="mb-2 flex justify-between text-xs font-bold text-slate-500"><span>Step {step} of {total}</span><span>{Math.round(step / total * 100)}%</span></div><div className="h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-sky-600 transition-all" style={{width:`${step/total*100}%`}}/></div></div> }

import type { ReactNode } from 'react'
import { Card } from '../ui/Card'
export function SymptomForm({ children }: { children: ReactNode }) { return <Card className="mx-auto max-w-2xl p-5 sm:p-8">{children}</Card> }
